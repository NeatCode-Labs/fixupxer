# Robolectric 4.14.1 supports emulated SDKs only up to 35 (Baklava/36 needs
# Robolectric 4.16+ and a JDK 21 test runtime). With targetSdk = 36, tests
# without an explicit @Config(sdk = ...) would otherwise default to SDK 36
# and fail to resolve an emulated runtime. Pin the default to 35.
sdk=35
