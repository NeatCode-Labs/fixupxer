History fix for Private Link Guard false positives: links whose sensitive input (e.g. Reddit out.reddit.com or Google Ads wrappers with token/sig parameters) is fully cleaned to a safe result now leave a redacted history entry holding only the safe final URL. The original URL with the token is still never saved. Links whose cleaned result still contains sensitive data continue to stay out of history and cache entirely.
