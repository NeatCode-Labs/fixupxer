Choose separate Browser readers, embeds or custom frontends for seven platforms, including TikTok and Instagram. Open with > FixupXer sends the same final URL to Copy, Share and Open. Existing reader choices migrate safely; backup v2 still imports v1. Failed or interrupted actions stay local for Retry. Host conversion preserves query and fragment data. All processing remains offline with zero permissions.
